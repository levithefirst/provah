export default function FinalCta() {
  return (
    <section className="bg-neutral-50 py-24">
      <div className="mx-auto max-w-4xl px-6 text-center">
        <h2 className="text-3xl font-semibold tracking-tight text-neutral-900 sm:text-4xl">
          Private state in. Portable capability out.
        </h2>
        <p className="mt-4 text-neutral-600">
          Consumed exactly once, from anywhere. Try the whole flow yourself — it takes under two
          minutes and it&apos;s real mainnet, not a testnet fork.
        </p>
        <a
          href="#app"
          className="mt-8 inline-block rounded-full bg-neutral-900 px-8 py-4 text-sm font-semibold text-white shadow-lg shadow-neutral-900/10 transition-transform hover:-translate-y-0.5 hover:bg-neutral-800"
        >
          Try the live demo
        </a>
      </div>
    </section>
  );
}
